# 更新日志

本文件记录本项目各版本的变更。版本号规则见 [`AssemblyInfo.vb`](Ruijie-WPF/My%20Project/AssemblyInfo.vb)。

## v2.0.0 (2026-09-17)

首个发布版本。

### 功能

- 图形化界面，一键连接 / 断开校园网 ePortal 认证
- 只需填写学号、密码与网络类型，认证参数全部在运行时向学校门户自动获取
- 自动挑选校园网网卡作为出口，避开 VPN、代理等虚拟网卡的干扰
- 断线自动重连，检测间隔可配置（1–99 秒）
- 密码使用 Windows DPAPI 加密保存，配置文件中无明文
- 支持开机自启（Windows 注册表自启）
- 事件日志实时显示，同时写入本地日志文件
- 网络监控线程后台运行，不阻塞 UI

### 说明

- 配置文件采用 v4 结构，只保存学号、加密后的密码、网络类型与自动重连开关
- 认证服务器地址、`queryString`、Cookie、密码密文等均为运行期数据，不写入配置文件
- 失败原因按 网络 / 认证参数 / 账户 / 门户 分类给出，便于定位问题

### 测试

- 142 项离线单元测试（`--test`），覆盖密码加密算法、门户发现、认证参数组装、
  配置读写、密码保护、出口地址解析等模块
- 提供真实环境验收入口（`--accept`），可在真实校园网下逐阶段验证完整认证链路
